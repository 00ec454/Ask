<manifest package="com.vistrav.ask"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <activity android:name=".AskActivity"/>
        <receiver android:name=".Ask$Receiver" android:enabled="true" android:exported="false">

            <intent-filter>
                <action android:name="com.vistrav.ask.PERMISSION_RESULT_INTENT"/>
            </intent-filter>
        </receiver>
    </application>
</manifest>
